# Payment Service (generated from specs)

## Layout

- **Source:** `src/PaymentService.Api`
- **Tests:** `tests/PaymentService.Tests`
- **Solution:** `PaymentService.slnx`

## Commands

From this directory:

```bash
dotnet restore
dotnet test
dotnet run --project src/PaymentService.Api
```

SSOT inputs live under `../specs` and `../prompts` (relative to `SpecStructure`).
